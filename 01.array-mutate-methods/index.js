// Array mutate methods in JS:
// Array.prototype.pop();
// Array.prototype.push();
// Array.prototype.shift();
// Array.prototype.unshift();
const arr = [1, 2, 3, 4, 5];

const length = arr.unshift(0);

console.log(arr);
console.log(length);
